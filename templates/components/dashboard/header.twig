<header class="bg-card border-border border-b">
    <div class="container-app flex items-center justify-between py-4">
        <div>
            <h1 class="text-2xl font-bold">Dashboard</h1>
            <p class="text-sm text-muted-foreground">
                Welcome back, <span id="user-name">User</span>
            </p>
        </div>
        <button onclick="window.logout()" class="inline-flex items-center justify-center rounded-md border border-input bg-background px-4 py-2 text-sm font-medium hover:bg-accent hover:text-accent-foreground transition-colors">
            {% include 'components/icons/log-out.twig' with { size: 16, class: 'mr-2' } %}
            Logout
        </button>
    </div>
</header>
